# Check-Your-PE
>this software allows you to analyze a file for malware, the program has an output in virus total, it also shows imports, exports and segments
>currently under active development
# build
```git clone https://github.com/DiggerMan-cpp/Check-YourPe.git
cd Check-YourPe
cmake -B build -A Win32
cmake --build build```
